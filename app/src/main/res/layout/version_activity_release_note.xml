<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="healthconnex.com.au.baseprojectpattern.ui.version.ReleaseNoteActivity">

    <TextView
        android:id="@+id/titleRelease"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:text="@string/label_title_version"
        android:textSize="@dimen/text_size_xxlarge"
        android:textColor="@color/theme_primary"
        />

    <View
        android:id="@+id/divLine"
        android:layout_height="1dp"
        android:layout_width="fill_parent"
        android:background="@color/theme_primary"
        android:layout_below="@+id/titleRelease"
        android:layout_marginTop="5dp"/>

    <ListView
        android:id="@+id/releaseNoteList"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@+id/divLine"
        android:layout_marginTop="15dp"
        android:layout_marginLeft="25dp"/>

</RelativeLayout>
